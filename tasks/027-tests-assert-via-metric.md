# Task 027: Tests Assert Via Metric

## Objective
Verify tests assert using metric object methods rather than raw values.

## Instructions
1. Grep `framework/_reference/tests/test_rag_pipeline.py` for `is_above_threshold` or `get_score`.
2. Confirm at least one match is found.

## Acceptance Criteria
- [ ] `is_above_threshold` or `get_score` appears in `framework/_reference/tests/test_rag_pipeline.py`

## Gate
Satisfies: TEST-04
