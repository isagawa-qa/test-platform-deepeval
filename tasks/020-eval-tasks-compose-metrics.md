# Task 020: Eval Tasks Compose Metrics

## Objective
Verify eval task files import from the metrics layer.

## Instructions
1. Grep all `.py` files in `framework/_reference/tasks/` for imports from `_reference.metrics`.
2. Confirm at least one match is found.

## Acceptance Criteria
- [ ] At least one task file imports from `_reference.metrics`

## Gate
Satisfies: TASK-02
