# Task 017: Metric Objects Return Self

## Objective
Verify metric objects use fluent interface pattern by returning self.

## Instructions
1. Grep all `.py` files in `framework/_reference/metrics/` for `return self`.
2. Confirm at least one match is found.

## Acceptance Criteria
- [ ] `return self` appears in at least one metric file under `framework/_reference/metrics/`

## Gate
Satisfies: METRIC-04
