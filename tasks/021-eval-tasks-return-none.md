# Task 021: Eval Tasks Return None

## Objective
Verify eval task files use `return None` to signal side-effect-only execution.

## Instructions
1. Grep all `.py` files in `framework/_reference/tasks/` for `return None`.
2. Confirm at least one match is found.

## Acceptance Criteria
- [ ] `return None` appears in at least one task file under `framework/_reference/tasks/`

## Gate
Satisfies: TASK-03
