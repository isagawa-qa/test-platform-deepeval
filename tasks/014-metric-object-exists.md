# Task 014: Metric Object Files Exist

## Objective
Verify the metrics directory contains at least one Python metric file.

## Instructions
1. Check that `framework/_reference/metrics/` directory exists.
2. List `.py` files in the directory (excluding `__init__.py`).
3. Confirm at least one metric `.py` file exists.

## Acceptance Criteria
- [ ] `framework/_reference/metrics/` contains at least one `.py` file (excluding `__init__.py`)

## Gate
Satisfies: METRIC-01
