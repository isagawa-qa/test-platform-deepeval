# Task 016: Metric Objects Have is_above_threshold

## Objective
Verify metric objects implement the `is_above_threshold` state-check method.

## Instructions
1. Grep all `.py` files in `framework/_reference/metrics/` for `def is_above_threshold`.
2. Confirm at least one match is found.

## Acceptance Criteria
- [ ] `def is_above_threshold` appears in at least one metric file under `framework/_reference/metrics/`

## Gate
Satisfies: METRIC-03
