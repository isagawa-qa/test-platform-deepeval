# Task 023: Eval Roles Compose Tasks

## Objective
Verify eval role files import from the tasks layer.

## Instructions
1. Grep all `.py` files in `framework/_reference/roles/` for imports from `_reference.tasks`.
2. Confirm at least one match is found.

## Acceptance Criteria
- [ ] At least one role file imports from `_reference.tasks`

## Gate
Satisfies: ROLE-02
