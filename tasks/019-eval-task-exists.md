# Task 019: Eval Task Files Exist

## Objective
Verify the tasks directory contains at least one Python task file.

## Instructions
1. Check that `framework/_reference/tasks/` directory exists.
2. List `.py` files in the directory (excluding `__init__.py`).
3. Confirm at least one task `.py` file exists.

## Acceptance Criteria
- [ ] `framework/_reference/tasks/` contains at least one `.py` file (excluding `__init__.py`)

## Gate
Satisfies: TASK-01
