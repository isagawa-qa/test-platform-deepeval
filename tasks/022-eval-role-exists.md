# Task 022: Eval Role Files Exist

## Objective
Verify the roles directory contains at least one Python role file.

## Instructions
1. Check that `framework/_reference/roles/` directory exists.
2. List `.py` files in the directory (excluding `__init__.py`).
3. Confirm at least one role `.py` file exists.

## Acceptance Criteria
- [ ] `framework/_reference/roles/` contains at least one `.py` file (excluding `__init__.py`)

## Gate
Satisfies: ROLE-01
